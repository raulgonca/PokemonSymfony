{% extends 'base.html.twig' %}

{% block title %}Pokedex{% endblock %}

{% block body %}
    <h1>Pokedex</h1>

    <table class="table">
        <tbody>
         <tr>
                <th>Nombre</th>
                <td>{{ pokedex.pokemon.name }}</td>
            </tr>
            <tr>
                <th>Nivel</th>
                <td>{{ pokedex.pokemonLevel }}</td>
            </tr>
            <tr>
                <th>Fuerza</th>
                <td>{{ pokedex.pokemonStrength }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pokedex_index') }}">back to list</a>

    <a href="{{ path('app_pokedex_train', {'id': pokedex.id}) }}">Entrena tu Pokémon!</a>
{% endblock %}
